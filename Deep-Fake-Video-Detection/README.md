# DeepFake Video Detection & LSTM Time Series Prediction

## Overview
- **DeepFake Detection:** Classifies videos as REAL or FAKE using InceptionV3 + GRU layers.  
- **Time Series Prediction:** Predicts next values in sequences using an LSTM model on sine wave data.

## Requirements
```bash
pip install opencv-contrib-python imageio tensorflow matplotlib pandas numpy
